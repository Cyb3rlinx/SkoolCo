// Placeholder page — replace with the real frontend.
// The backend lives under /api (see README.md for the full contract).
export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>LaunchPad API</h1>
      <p>Backend is running. See README.md for API documentation.</p>
    </main>
  );
}
